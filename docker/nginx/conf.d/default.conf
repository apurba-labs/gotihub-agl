limit_req_zone $binary_remote_addr zone=gotihub_limit:10m rate=10r/s;

server {
    listen 80;
    server_tokens off;
    index index.php index.html;
    error_log  /var/log/nginx/error.log;
    access_log /var/log/nginx/access.log;
    root /var/www/public; # Laravel's entry point

    # Handle the main requests
    location / {
        limit_req zone=gotihub_limit burst=20 nodelay;
        try_files $uri $uri/ /index.php?$query_string;
        gzip_static on;
    }

    # Pass PHP scripts to PHP-FPM (The 'app' container)
    location ~ \.php$ {
        proxy_read_timeout 300;
        proxy_connect_timeout 300;
        proxy_send_timeout 300;
        fastcgi_read_timeout 300;
        try_files $uri =404;
        fastcgi_split_path_info ^(.+\.php)(/.+)$;
        fastcgi_pass app:9000; # 'app' matches your service name in docker-compose
        fastcgi_index index.php;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param PATH_INFO $fastcgi_path_info;
    }

    # Deny access to sensitive files
    location ~ /\.(?!well-known).* {
        deny all;
    }
}